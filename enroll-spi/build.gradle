subprojects {
    dependencies {
        implementation project(':enroll-pojo')
        api 'org.springframework.cloud:spring-cloud-starter-openfeign:' + project.dependencyVersions.springcloud
    }

}